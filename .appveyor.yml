# Test against the latest version of this Node.js version
environment:
  nodejs_version: "11"

# Install scripts. (runs after repo cloning)
install:
  # Get the latest stable version of Node.js or io.js
  - ps: Install-Product node $env:nodejs_version
  # install modules
  - npm install

build_script:
  - npm build


deploy:
  provider: FTP
  protocol: sftp
  host: igloo15.com
  username: igloo15
  password:
    secure: G+1uj5hFgNOeSb8uWVeW5hzqqYOl7doFOnGctMC2C7k=
  folder: dist

branches:
  # Whitelist
  only:
    - develop
    - master
    - /release/.*/
    - /hotfix/.*/

cache:
- tools -> recipe.cake, tools/packages.config
